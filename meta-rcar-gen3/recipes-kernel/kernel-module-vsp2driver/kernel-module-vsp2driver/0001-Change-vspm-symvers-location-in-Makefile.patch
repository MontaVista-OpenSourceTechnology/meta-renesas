Upstream-Status: Inappropriate [configuration]

diff --git a/vsp2driver/Makefile b/vsp2driver/Makefile
index 7603ee8..1a48be2 100644
--- a/vsp2driver/Makefile
+++ b/vsp2driver/Makefile
@@ -19,7 +19,7 @@ EXTRA_CFLAGS += $(U_INCLUDE)
 EXTRA_CFLAGS += -DUSE_BUFFER
 
 target:
-	make -C $(KERNELDIR) M=$(PWD) KBUILD_EXTRA_SYMBOLS=$(KERNELSRC)/include/vspm.symvers modules
+	make -C $(KERNELDIR) M=$(PWD) KBUILD_EXTRA_SYMBOLS=$(KERNELSRC)/vspm/vspm.symvers modules
 
 all:
 	make clean
